# 实体翻页按键验收（2026-09-16）

- BOOT/GPIO9上一页，KEY/GPIO10下一页，仅在大厅且无亮度弹窗时有效。40 ms消抖、按住不重复、双键同按忽略、首页末页边界限制。
- 主机GPIO模拟驱动真实LVGL回调，验证消抖、按住、左右翻页、双键、首末页边界、详情与弹窗隔离；原160次混合进出及100轮导航测试通过。
- ESP-IDF构建及COM5应用分区烧录通过，哈希校验通过；物理按键方向及实际短按响应待用户确认。

# 电量显示验收（2026-09-16）

- 大厅顶部新增电量百分比及供电/充电状态，每10秒刷新；低电量黄色、充电绿色，未知与未连接状态明确区分。
- Luna实现AXP2101读取接口，主代理检查寄存器定义、修正USB状态判断并将查询超时限制为20 ms，无新增后台任务及充电参数更改。
- 真实LVGL主机测试覆盖充电、未接电池、读取失败、低电量放电；160次混合游戏进出、100轮导航通过，截图已检查。
- ESP-IDF构建通过，应用1,056,528字节；COM5仅更新应用分区，哈希校验通过。
- 实机启动读取valid=yes connected=yes charging=no usb=yes percent=100；九款游戏大厅启动正常，无panic，2048存档score=496、best=496保留。日志见firmware/board-startup.log。
- USB插拔及完整充放电过程未做实测，百分比为芯片估计值。

# 关卡难度调整（2026-09-15）

- 根据用户实机反馈重做三关，Luna max调整布局，主代理完成路径、界面及构建验收。
- 三关分别使用3/4/5堵连接边界的墙，交替开口宽72/52/36像素，球直径20像素，封闭原来的外围捷径。
- 4像素网格按完整球体碰撞范围搜索，三关最短安全路径分别952/1540/1688像素，全部可达且递增。测试为关卡长度设下限，旧布局不能通过。
- 真实LVGL界面三关截图已检查；校准、方向、暂停、传感器丢失恢复、三关通关与100次进出测试通过，堆52,432字节前后不变。
- ESP-IDF构建通过，应用1,049,712字节，未新增运行内存分配；实机新难度手感待用户反馈。

# 平放校准修复（2026-09-15）

- 用户反馈平放校准进度反复跳动、无法完成。原代码直接将未经校准的角速度与0.10 rad/s比较，偏置叠加轻微噪声会反复清零。
- 已用真实LVGL界面测试复现：给静止输入加入固定偏置和交替噪声，旧实现失败于neutral calibration collects samples。
- 修复为在40个样本的采集期间检查加速度及角速度相对首样本的稳定性，平均角速度作为零偏；保留量程合理性和明显运动限制。
- 同一回归测试通过，并新增持续晃动不能完成校准的检查；姿态融合、三关交互和100次进出测试通过。
- ESP-IDF构建通过，应用1,049,712字节；COM5应用分区烧录哈希校验通过，重启确认传感器与九个大厅条目正常，2048存档保留。修复后的实机校准操作仍待用户确认。

# 重力滚球验收记录

日期：2026-09-15。Luna 完成物理引擎初稿，主代理完成修正、姿态融合、硬件接入、界面及验收。

- 大厅第三页新增重力滚球，共九款游戏；三关迷宫，倾斜控制、暂停、重新校准、通关及重玩。
- QMI8658 加速度与角速度共同参与姿态估计；平放、右侧下倾、下侧下倾三步校准确定操作方向。
- 引擎测试覆盖速度限制、摩擦、边界与墙壁碰撞、三关路径可达性及通关状态；输入测试覆盖单位转换、陀螺仪作用、偏置、无效数据与方向校准。
- 真实 LVGL 主机测试通过：校准、移动、暂停、传感器丢失与恢复、三关通关及100次进出；可用堆52,432字节前后不变，峰值19,256字节。
- 集成测试覆盖第三页入口，另完成原八款游戏160次混合进出及100轮导航；主机数据不代表整机空闲内存。
- ESP-IDF 5.5.3 构建通过，应用1,049,152字节，6 MiB应用分区剩余约83%。
- COM5仅更新应用分区，烧录哈希校验通过。重启确认九个大厅条目、显示与触摸初始化成功，2048存档score=496、best=496仍在。
- 实机QMI8658地址0x6b，启动读到加速度(-0.053, 0.002, -1.047) g及角速度(0.183, 0.048, 0.008) rad/s。初始化等待新数据最多800 ms；本次约140 ms取得数据。
- 15秒启动日志无panic或反复重启，见firmware/board-startup.log。实际手持校准与倾斜手感待用户确认，未宣称已完成人工操作验收。

以下保留此前版本验收记录。

# 八款游戏完整版验收记录

日期：2026-09-14。本次四款游戏由主代理完成；Luna 服务容量不足，未参与本次实现。

## 功能与规则

- 打砖块：24 砖、3 生命、点击发球、拖动挡板、暂停/继续、重开、清砖胜利。
- 记忆翻牌：16 卡/8 对数字，匹配保留，错配显示 700 ms，等待期间锁定第三张输入，计步与通关。
- 扫雷：6×6/6 雷，首次开格及周围安全，空白展开、模式按钮开格/插旗、6 面旗上限、胜负与重开。
- 反应挑战：随机等待 1.5–4.5 秒，绿色及文字提示后按下记录，抢跑/超时不计入，5 次有效结果取平均；取消正在计时的尝试不会产生有效成绩。
- 四款均使用中文、大触控区域，进入新局、退出释放界面。无新增断电存档；保留原有 2048 存档。

## 自动与视觉验收

- 打砖块：墙面、砖块、挡板反弹、生命/发球、胜利与暂停规则，以及 50,000 帧物理压力测试通过。
- 翻牌：1,000 次洗牌每种数字恰好一对，1,000 次完整解局、同卡/非法输入、700 ms 锁定与重开通过。
- 扫雷：7,200 种种子/首格组合验证安全邻域、雷数和全部安全格可打开；旗保护/数量上限、失败与重开通过。
- 反应：抢跑、等待区间、五次计分、均值、时钟回绕、取消、超时与重开通过。
- 四款真实 LVGL 主机指针测试均通过，覆盖主要交互、胜负/成绩、弹窗和各 100 次进出；无新增存活内存块。
- 八款游戏混合进入退出 160 次通过；随后 100 轮大厅导航/亮度通过。混合前后可用堆 31,728 → 31,768 字节；导航前后 31,800 → 31,808 字节。
- 修复扫雷返回大厅时旧画面与新大厅重叠分配造成的内存不足。导航改为延迟回调中先释放旧画面，再构建新画面，仍使用 64 KiB LVGL 池。综合测试峰值 39,280 字节（主机数据）。
- 中文字体重新生成；四款主机渲染截图逐一检查。截图不是设备实拍，主机数据不等同于整机空闲 RAM。

## 固件与设备

- ESP-IDF 5.5.3 / ESP32-C6 最终构建与分区检查通过，应用 990,176 字节，6 MiB 分区剩余约84%。
- COM5 仅烧录 0x10000 应用分区，Flash 哈希校验通过，NVS 存档保留。
- 15 秒重启日志确认显示、触摸、LVGL 和8个大厅条目启动正常，无 panic 或重复重启；记录见 firmware/board-startup.log。
- 本次实机交互手感待用户反馈；没有进行长期连续运行、续航或反应测试计时精度的硬件标定。

# 灯泡全灭与叠叠高验收记录

日期：2026-09-14。灯泡全灭由 Luna max 开发，主代理完成布局修正、叠叠高开发、集成与验收。

- 首页四款游戏可玩：2048、贪吃蛇、灯泡全灭、叠叠高；第二页四款待开发。
- 灯泡全灭：5×5，60 px 触控格，切换自身和四邻；合法步骤打乱生成，支持重置、确认换关、通关下一关。
- 灯泡规则测试通过：角/边/中心翻转、非法索引、重置、升级和 1,000 个随机种子的独立求解验证。
- 灯泡真实 LVGL 指针测试通过：点击切换、重置、换关弹窗拦截/取消/确认、触摸解完整棋盘、下一关、100 次进出；可用堆 52,424 → 52,416 字节，无新增存活块。
- 叠叠高：点击开始/落块，重叠裁切，3 px 内完美吸附，80–210 px/s 逐渐提速，自动跟随高塔，暂停/继续/确认重开/结束。
- 叠叠高规则测试通过：计时、边缘反弹、裁切、暂停、1,000 层完美堆叠、未命中结束；真实 LVGL 指针与100次进出通过，可用堆 52,424 → 52,400 字节，无新增存活块。
- 大厅 100 轮导航/亮度回归，以及 2048、贪吃蛇各 100 次进出与交互回归通过。大厅测试内存峰值 48,320 字节，LVGL 池容量 65,536 字节；此为主机数据。
- 中文字体重新生成，布局由真实 LVGL 主机渲染截图检查。截图不是实拍。
- ESP-IDF 5.5.3 构建与分区检查通过；应用 938,528 字节（6 MiB 分区剩余约85%）。
- COM5 仅更新 0x10000 应用分区，Flash 哈希校验通过，2048 NVS 存档保留。
- 重启观察15秒：面板、触摸及LVGL初始化正常，日志确认8个大厅条目，无panic或重复启动；见firmware/board-startup.log。
- 新游戏实机触摸手感待用户反馈；此前贪吃蛇已由用户确认可以正常游玩。
- 两款新游戏不保存断电进度；叠叠高最高层数仅保留本次开机期间。没有进行长期续航测试。

# 贪吃蛇验收记录

日期：2026-09-14。Luna max 开发初稿；主代理完成后续修正、测试、构建与烧录。

- 全屏滑动转向；16×16 棋盘、24 px 格子、22 px 蛇身；初速 300 ms，每个食物加快 10 ms，最快 140 ms。
- 准备态等待首次有效滑动；禁止反向和同一步多次转向；支持暂停/继续、确认重开、撞墙/撞身结束及满盘胜利。
- 贪吃蛇每次进入开始新局，最高分在本次开机期间保留；2048 保持原有 NVS 存档功能。
- 纯引擎规则测试通过：计时、开局转向锁、反向限制、暂停、增长、进入刚移开的尾格、撞身/撞墙和重开。
- 满盘压力测试通过：16,919 步真实食物生成与增长，最终长度 256、得分 253；每步检查身体唯一、食物不重叠和速度下限。
- 真实 LVGL 9.5.0 主机测试通过：HUD 区域滑动、轻点不启动、按钮内滑动不点击且释放按下状态、暂停计时、弹窗滑动不恢复、确认重开、死亡和 100 次进出。可用堆前后均为 52,400 字节，无新增存活内存块。
- 大厅 100 轮导航/亮度回归通过，两个游戏入口可用；2048 的合并、撤销、存档、胜利/终局及 100 次进入退出回归通过。
- ESP-IDF 5.5.3 最终构建通过，应用 905,408 字节，目标 ESP32-C6。中文字体已重新生成。
- COM5 仅更新应用地址 0x10000，Flash 哈希校验通过，NVS 分区保留。
- 实机手势手感、速度适配等待用户反馈；主机截图不是实拍。未进行长时间连续运行或续航测试。

# 2048 历史验收记录

日期：2026-09-13。开发：Luna max；审查、规则引擎与验收：主代理。

## 构建与软件验收

- ESP-IDF v5.5.3、ESP32-C6、LVGL 9.5.0；固件构建与分区检查通过。
- 应用 877,200 字节；16 MiB Flash、6 MiB 应用分区；480×480 RGB565，LVGL 堆 64 KiB，不依赖 PSRAM。
- 规则测试：四向移动、每步单次合并、无效移动不生成数字、撤销恢复随机状态、胜利继续、终局、损坏存档拒绝，以及 2,000 次移动尝试的数字总量检查通过。
- 真实 LVGL 主机 UI 测试：滑动/点击区分、撤销、重开确认、保存返回、胜利继续、终局和 100 次进入退出通过；可用堆前后均为 52,400 字节。
- 大厅回归：2048 入口、分页返回、亮度和弹窗拦截通过。100 轮后存活块数保持 393，可用堆 30,992 → 30,936 字节；延长至 200 轮为 31,056 字节。小幅变化来自分配器碎片，断言检查存活块数不增加及碎片开销不超过 512 字节。
- 实际存储模块配合 NVS 替身测试：读写、CRC/schema/长度校验、错误处理、句柄释放、初始化失败不擦除数据通过。
- 中文字符覆盖检查通过；主机渲染截图仅代表软件布局，不是实拍。

## 实机记录

- COM5，ESP32-C6 revision v0.2；仅更新 0x10000 应用分区，保留 NVS。
- 2048 固件烧录完成，Flash 哈希校验通过。
- 启动日志确认 NVS ready、PMIC/显示初始化成功、触摸 0x9220 / 480×480、LVGL 任务和中文大厅启动成功。
- 15 秒启动观察未见 panic、watchdog 或重复启动。记录见 firmware/board-startup.log。
- 此前用户已确认中文大厅显示与触摸正常；本次 2048 实机手势及断电恢复仍待用户反馈。软件模拟存档测试不等同于实机断电测试。

2048 支持滑动合并、单步撤销、确认重开、2048 后继续、终局提示和 NVS 自动存档。移动后延迟 900 ms 保存，返回立即保存；尚未保存时断电可能丢失最近一步。当时其余五个游戏仍为待上线。未进行长时间续航、功耗或音效测试。

最新贪吃蛇固件启动观察：NVS ready、PMIC、面板、触摸及 LVGL 初始化成功；15 秒日志未出现 panic、watchdog 或重复重启。见 firmware/board-startup.log。
